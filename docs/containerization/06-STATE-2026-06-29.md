# TTS Lab Deployment State — 2026-06-29

> Previous state: `05-STATE-2026-06-21.md`
> Commit: `eb48b67`

## Running Containers

| Container | Status | Port | Notes |
|---|---|---|---|
| tts-lab-orchestrator | Up (unhealthy — curl check port mismatch) | 8009 | Full 28 engine URLs + Docker socket |
| tts-lab-engine-current | Up (healthy) | 8101 | 23 engines available. torchcodec shim active via docker cp |
| tts-lab-engine-qwen | Up (healthy) | 8104 | Qwen3TTS only |

## Stopped Containers

| Container | Why |
|---|---|
| tts-lab-llm-qwen36 | Manually stopped to free VRAM for TTS. Auto-restart by orchestrator on LLM inference (Docker socket). |
| tts-lab-engine-mid | Not deployed (profile: mid). VibeVoice/Higgs routed here when started. |
| tts-lab-engine-legacy | Not deployed (profile: legacy). IndexTTS/Parler routed here when started. |
| tts-lab-orpheus | Not deployed (profile: gpu). Needs CUDA 12.1 + vllm. |
| tts-lab-sglang / vibevoice / higgs / s2pro | Not deployed (profile: sglang). SGLang engines. |

## Engine Availability (30 total)

**19 Available:**
piper, kokoro, melo, matcha, chattts, outetts, bark, styletts2, f5tts, dia, xtts, chatterbox, chatterboxturbo, fishspeech, csm, mmsfas, zonos, omnivoice, qwen3tts

**11 Unavailable (all expected):**
cosyvoice (not installed), manatts (not installed), openvoice (not installed), neutts (not configured),
higgs (no container), orpheus (no container), s2pro (no container), vibevoice (no container),
indextts (no container), parler (no container), qwen36 (LLM stopped)

## GPU (RTX 5060 Ti 16 GB)

- **VRAM free:** ~12 GB (LLM stopped, engine-qwen ~3.7 GB loaded)
- **Engine-qwen:** Qwen3TTS loaded (~3.7 GB)
- **Engine-current:** No model loaded (lazy-loading)

## Key Configuration

### Orchestrator Env Vars (all 28 set)
```
PIPER_URL, MATCHA_URL, MELO_URL, KOKORO_URL, CHATTTS_URL, BARK_URL,
OUTETTS_URL, STYLETTS2_URL, F5TTS_URL, DIA_URL, XTTS_URL, COSYVOICE_URL,
FISHSPEECH_URL, CHATTERBOX_URL, CHATTERBOXTURBO_URL, OMNIVOICE_URL,
OPENVOICE_URL, ZONOS_URL, MANATTS_URL, MMSFAS_URL, CSM_URL,
QWEN3TTS_URL, VIBEVOICE_URL, HIGGS_URL, INDEXTTS_URL, PARLER_URL,
ORPHEUS_URL, S2PRO_SGLANG_URL, QWEN36_URL
```

### Docker Socket
Mounted at `/var/run/docker.sock` in orchestrator. Enables LLM container start/stop via Docker API.

## Recent Fixes (2026-06-27/29)

1. **Remote engine routing** — Added 21 missing `_URL` env vars to orchestrator. All engines now route via HTTP.
2. **torchcodec ASR crash** — Patched `is_torchcodec_available` → `False` in shims. OmniVoice voice cloning works.
3. **LLM VRAM coordination** — Docker socket auto-eviction. Heavy TTS stops LLM; LLM restarts on inference.

## Known Issues

- **dia, styletts2**: Hang >180s (pre-existing, not investigated)
- **Orchestrator healthcheck**: Reports unhealthy — the check uses port 8001 but the container runs on 8009. Cosmetic only.
- **Engine-current image**: Fixes applied via `docker cp` (not baked into image yet). Docker daemon has 2 stuck build processes — needs `systemctl restart docker` or VM reboot before `make build-engine ENGINE=current` will work.

## Next Steps

1. Restart Docker daemon (clear stuck processes) → `make build-engine ENGINE=current`
2. Start LLM container → verify auto-eviction/restart cycle works end-to-end
3. Investigate dia/styletts2 timeout
4. Deploy engine-mid/engine-legacy containers if needed
5. Rebuild engine-current image to bake shims fix
